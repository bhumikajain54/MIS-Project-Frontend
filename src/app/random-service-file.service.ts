import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class RandomServiceFileService {

  // constructor(private http: HttpClient) { }

  // callRandomPost(url: string){
  //   return this.http.get(url);
  // }
}
